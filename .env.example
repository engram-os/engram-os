DB_PASSWORD=
OPENAI_API_KEY=
PORT=3000

# Engram OS — Security
# Generate once with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Must be the same value across all containers (os_layer, celery_worker).
ENGRAM_ENCRYPTION_KEY=
ENGRAM_API_KEY=
AUDIT_HMAC_SECRET=
LINEAR_KEY=dummy_linear_api_key_1234567890
JIRA_URL=https://dummycompany.atlassian.net
JIRA_EMAIL=dummyemail@example.com
JIRA_TOKEN=dummy_jira_api_token_9876543210